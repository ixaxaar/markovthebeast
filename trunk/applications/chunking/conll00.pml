predicate word:     Int x Word;
predicate prefix:   Int x Prefix;
predicate postfix:  Int x Postfix;
predicate case:     Int x Case;
predicate cardinal: Int x Cardinal;
predicate pos:      Int x Pos;
predicate chunk:    Int x Int x Chunk;

index: chunk(*,*,_);
