package com.googlecode.thebeast.world;

/**
 * A UserPredicate is a predicate that is defined by the user. Also, in contrast to a built-in predicate it can have
 * different relations attached to it in different possible worlds. <p/> <p>UserPredicates can only be created by {@link
 * Signature}.
 *
 * @author Sebastian Riedel
 */
public interface UserPredicate extends Predicate {
}
